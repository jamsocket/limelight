Use [trunk](https://github.com/thedodd/trunk) to run these examples, as in:

```bash
$ cd animate-triangle
$ trunk serve --release
```

`--release` is recommended because WASM code runs pretty slow without it.